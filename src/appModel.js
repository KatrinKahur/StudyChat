class AppModel{
    /*constructor(){
        this.observers = []
        this.userList = firebase.database().ref('userList');
    };

    //kopierade från testlabben i interaktionsprogrammeringen. kanske behöver ändras på
    addObserver(callback){
        this.observers = [...this.observers, callback]
    }

    removeObserver(callback){
        function removeCheck(arg){
            return callback !== arg
        }

        this.observers.filter(removeCheck)
    }

    notifyObservers(){
        this.observers.forEach(cb => {try{cb()} catch(error) {console.log(error)}})
    }

    setMainScreen(userList){
        this.userList = userList;
    }*/
}